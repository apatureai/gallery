{
  "name": "meridian-gallery",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Meridian — a small marketing site and deploy dashboard, used as the reference app for Apature Gate design reviews.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "node scripts/preview.mjs",
    "check:tokens": "node scripts/check-tokens.mjs"
  },
  "devDependencies": {
    "vite": "6.4.3"
  }
}
